name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test-python:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.10', '3.11', '3.12', '3.13']

    steps:
      - uses: actions/checkout@v5
        with:
          path: nova

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        working-directory: nova
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Verify package install
        working-directory: nova
        run: python -c "import nova; print(nova.__file__)"

      - name: Run Python tests
        working-directory: nova
        run: python -m pytest tests/ -v --tb=short

      - name: Run test with coverage
        if: matrix.python-version == '3.12'
        working-directory: nova
        run: python -m pytest tests/ -v --cov=nova --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v5
        with:
          files: ./nova/coverage.xml
          fail_ci_if_error: false

  test-c-runtime:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          path: nova

      - name: Install build tools
        run: sudo apt-get update && sudo apt-get install -y gcc make

      - name: Build C runtime
        working-directory: nova/runtime
        run: make test_runtime

      - name: Run C runtime tests
        working-directory: nova/runtime
        run: ./test_runtime

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          path: nova

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install lint tools
        working-directory: nova
        run: |
          python -m pip install --upgrade pip
          pip install ruff

      - name: Run Ruff linter
        working-directory: nova
        run: ruff check . || true

      - name: Run Ruff formatter check
        working-directory: nova
        run: ruff format --check . || true
