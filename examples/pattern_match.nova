type Shape {
  Circle(r: Float)
  Rect(w: Float, h: Float)
}

fn area(s: Shape) -> Float {
  match s {
    Circle(r)    -> 3.14159 * r * r
    Rect(w, h)   -> w * h
  }
}

fn main() -> Unit {
  let c = Circle(5.0)
  print(float_to_str(area(c)))
}
