# Nova 前后端专项开发日志

本日志由前后端专项开发系统自动生成，记录每轮开发的详细信息。

---

## 🔍 第 6 轮评审 — 2026-07-22 22:03 ~ 22:15

> 三轮回顾评审：第 4-5 轮总结 + 双线路线图调整

---

### 📊 三轮回顾总结（第 4-5 轮 + 第 3 轮评审）

**完成任务统计：**

| 轨道 | 完成数 | 总任务数 | 完成率 |
|------|--------|---------|--------|
| 🎨 前端 | 2 | 6 | 33.3% → 66.7% |
| ⚙️ 后端 | 2 | 12 | 16.7% → 41.7% |
| **总计** | **4** | **18** | **22.2% → 50.0%** |

**两轮产出质量：** 4/4 全部成功，无失败任务，测试通过率保持 100%（392 passed），无回归。
**难度构成：** 2 easy + 1 medium + 1 hard（hard 任务首次成功验证）

---

### 🎨 前端线评估

**质量趋势：稳步提升**
- 两轮完成了 ADT 类型参数相等修复和 `==/!=` 类型检查修复，类型安全性稳步提升
- 类型检查器的 bug 修复效率很高（easy 任务半小时级）
- 但核心机制（类型合一）仍完全缺失，类型系统整体是"有壳无心"的状态

**进度评估：**

| 层面 | 完成度（第3轮时） | 完成度（现在） | 变化 |
|------|-----------------|--------------|------|
| 词法分析 | 90% | 90% | - |
| 语法分析 | 85% | 85% | - |
| AST 设计 | 90% | 90% | - |
| 类型系统（核心） | ~15% | ~35% | +20% |
| 类型系统（表面） | ~60% | ~70% | +10% |
| **前端整体** | ~45% | ~55-60% | +10% |

**新发现 Bug：**

| Bug | 严重程度 | 位置 | 说明 |
|-----|---------|------|------|
| TupleType 属性名不匹配 | 🟡 中 | type_checker.py 第1071-1075行 | `_types_compatible` 用 `element_types`，但 TupleType 属性是 `elements`，会抛 AttributeError |

**价值评估（当前任务池）：**

| 任务 | 价值评级 | 说明 |
|------|---------|------|
| 类型合一算法 | ⭐⭐⭐⭐⭐ | 类型系统的基石，没有它 TypeVar ≈ Any |
| TupleType 属性名修复 | ⭐⭐⭐⭐ | 低难度高收益，直接修复 AttributeError |
| 模式匹配完备性 | ⭐⭐⭐⭐ | 安全性高价值功能 |
| Map 字面量解析 | ⭐⭐⭐ | 填补功能断层 |
| 解析器错误恢复 | ⭐⭐⭐ | 开发体验提升，不影响正确性 |

**最大短板：类型合一算法缺失**
- 这是前端所有后续工作的前提
- 泛型函数、类型推断都是"假的"
- easy 高收益 bug 已基本清完，下一步必须面对 hard 任务

---

### ⚙️ 后端线评估

**质量趋势：框架快速搭建，但核心部件有设计缺陷**
- 两轮完成了 ELF 入口修复/接入管道 和 寄存器分配，原生后端从"僵尸代码"变成了"有骨架的原型"
- 但深度审计发现寄存器分配有**设计级 bug**，不是小修小补能解决的
- WasmGC 后端"名不副实"的问题被确认——GC 类型几乎没被实际使用

**进度评估：**

| 后端 | 完成度（第3轮时） | 完成度（现在） | 变化 | 阶段 |
|------|-----------------|--------------|------|------|
| C 后端 (lir_c_backend) | ~70% | ~75-80% | +5-10% | 功能可用阶段 |
| WasmGC 后端 | ~45% | ~45-50% | ±0 | 原型可用阶段 |
| 原生 x86_64 后端 | ~15% | ~30-35% | +15-20% | 原型有缺陷阶段 |

**重大发现：原生后端寄存器分配有设计缺陷**

第 5 轮实现的线性扫描寄存器分配器存在多个根本性问题：

1. **vname 命名错误（最严重）**：`LIRLoadConst` 用常量值本身命名 vreg（如 `const_42`），但分配结果应该绑定到 `dst_loc`（如 `r2`）。导致分配表键名与使用位置不匹配。
2. **浮点栈操作为占位**：浮点值从栈加载和存储都是 `pass` 空实现，涉及浮点溢出的代码全部错误。
3. **get_loc fallback 到 RAX**：找不到 vreg 时静默 fallback 到 RAX，会导致难以调试的数据损坏。
4. **ELF phnum 不匹配**：声明 3 个 program header 但只写了 2 个，ELF 解析器会越界读取。

**价值评估（当前任务池）：**

| 任务 | 价值评级 | 说明 |
|------|---------|------|
| 寄存器分配设计修复 | ⭐⭐⭐⭐⭐ | 最优先，没有正确的寄存器分配，原生后端等于不能用 |
| Phi 边缘块修复 | ⭐⭐⭐⭐ | 正确性 bug，影响所有后端 |
| 调用约定 | ⭐⭐⭐⭐⭐ | 函数不能传参 = 没有实用价值 |
| 栈帧管理 | ⭐⭐⭐⭐ | 寄存器分配的配套，溢出正确性依赖它 |
| ELF phnum 修复 | ⭐⭐⭐ | 低难度高收益的正确性修复 |
| Wasm 栈平衡验证 | ⭐⭐⭐ | 正确性基础 |

**最大短板：原生后端核心部件有设计缺陷**
- 寄存器分配器看起来工作了（测试通过），但实际上是因为测试用例太简单（单函数、无浮点、少变量）
- 一旦程序复杂度上升，bug 会集中爆发
- 需要在做栈帧和调用约定之前，先把寄存器分配修正确保正确

---

### 🧭 综合评估

**前后端平衡：后端缺口更大**

| 层面 | 完成度 |
|------|--------|
| 前端（解析 + AST） | 85% |
| 前端（类型系统核心） | ~35% |
| 中端（IR 三层 + Pass） | 65-70% |
| 后端（C） | 75-80% |
| 后端（Wasm） | 45-50% |
| 后端（原生） | 30-35% |

**判断**：前端类型系统和原生后端都是"硬骨头"，但原生后端的 bug 更紧急——已投入的代码量有设计缺陷，继续往上叠功能只会让债务越滚越大。建议**下 3 轮后端投入 65%，前端 35%**。

**方向正确性：**
- 前端方向正确，但类型合一这个硬骨头必须开始啃了
- 后端方向正确，但需要先"回退一步"修复寄存器分配，再继续前进
- 原生后端的优先级最高——这是 Nova 区别于其他教学语言的核心卖点

**效率评估：**
- 两轮 4 个任务全部成功，效率不错
- 首次尝试 hard 任务（寄存器分配）成功，但审计发现有设计缺陷
- 每轮平均产出：2 个中小型任务，1 轮可完成 1 个 hard + 1 个 easy/medium

---

### 📝 任务池变更说明

**前端任务池变更（6 → 6 个）：**

| 变更 | 任务 | 原因 |
|------|------|------|
| ➕ 新增 | 修复 TupleType 兼容检查属性名 bug（priority 85） | 审计发现的 AttributeError bug，easy 高收益 |
| ➖ 移除 | （无） | - |
| 🔄 状态 | ADT 类型参数相等 → completed | 第 4 轮完成 |
| 🔄 状态 | ==/!= 类型检查 → completed | 第 5 轮完成 |

**后端任务池变更（12 → 12 个）：**

| 变更 | 任务 | 原因 |
|------|------|------|
| ➕ 新增（P0） | 修复原生后端寄存器分配设计缺陷（priority 98） | 审计发现的设计级 bug，当前最高优先级 |
| ➕ 新增 | 修复 ELF phnum 不匹配 bug（priority 72） | 审计发现的 easy bug |
| ➖ 移除 | （无） | - |
| ⬆️ 优先级提升 | Phi 边缘块修复（82→86） | 确认是正确性 bug，影响所有后端 |
| 🔄 依赖调整 | 栈帧管理 → 依赖 regalloc_fix | 必须先修复寄存器分配，再做栈帧 |
| 🔄 依赖调整 | 指令选择优化 → 依赖 regalloc_fix | 同上 |
| 🔄 状态 | ELF 入口修复 + 接入管道 → completed | 第 4 轮完成 |
| 🔄 状态 | 寄存器分配 → completed（但有缺陷） | 第 5 轮完成，新增修复任务 |

---

### 🎯 下 3 轮方向与重点（第 7-9 轮）

**总体策略：先修 bug 再前进，后端攻坚 + 前端起步攻坚**

**后端（65% 精力）：**
1. 第 7 轮：修复寄存器分配设计缺陷（P0，最高优先级）
2. 第 8 轮：修复 Phi 边缘块问题（正确性，影响所有后端）
3. 第 9 轮：栈帧管理 + ELF phnum 修复（完善原生后端基础设施）

**前端（35% 精力）：**
1. 第 7 轮：修复 TupleType 属性名 bug（easy，快速清掉）
2. 第 8-9 轮：类型合一算法攻坚（hard，前端最核心任务，可能需要 2 轮以上）

---

### 更新后的进度概览

| 轨道 | 总数 | 已完成 | 进行中 | 完成率 |
|------|------|--------|--------|--------|
| 🎨 前端 | 6 | 4 | 0 | 66.7% |
| ⚙️ 后端 | 12 | 5 | 0 | 41.7% |
| **总计** | **18** | **9** | **0** | **50.0%** |

---

## 第 5 轮 — 2026-07-22 19:01 ~ 19:15

### 本轮概览
- **前端任务**: ✅ 修复 ==/!= 比较运算符类型检查 (easy, 75)
- **后端任务**: ✅ 实现原生后端线性扫描寄存器分配器 (hard, 94)
- **测试对比**: 开发前 392 passed → 开发后 392 passed（无回归）

---

### 🎨 前端：修复 ==/!= 比较运算符类型检查

**为什么选这个？**
- easy 难度，低风险高收益的类型安全修复
- 第 3 轮评审中明确标记为待修复的类型安全 bug
- 上一轮计划的"继续清理 easy 高收益类型安全 bug"的延续
- `==` 和 `!=` 直接返回 `BOOL_T`，完全不检查操作数类型，`"hello" == 42` 等荒谬比较都会通过

**实现详情**（`type_checker.py` 第 948-961 行）：

**1. 新增所有比较操作的左右类型兼容检查**
- 在 `==/!=/< > <= >=` 所有比较操作前，先检查 `_types_compatible(left_ty, right_ty)`
- 不兼容时抛出精确错误：`操作符 '==' 的操作数类型不兼容：String 和 Int`

**2. 修复有序比较的冗余检查**
- 原代码对 `< > <= >=` 分别检查左右操作数的 Int/Float 类型，但逻辑复杂且有 operator precedence 隐患
- 简化为：先检查左右类型兼容（已保证同类型），再检查左操作数是 Int 或 Float
- 错误信息更精确：`操作符 '<' 需要数值类型操作数，得到 String`

**结果**: ✅ 成功，测试全部通过

---

### ⚙️ 后端：实现原生后端线性扫描寄存器分配器

**为什么选这个？**
- 优先级 94（所有 pending 任务中最高），hard 难度但价值巨大
- 原生后端的核心瓶颈——之前硬编码 RAX/RCX 的玩具级实现无法支持任何真实程序
- 依赖 `backend_native_elf_entry_fix` 已在第 4 轮完成，依赖链畅通
- 是后续栈帧管理、调用约定、更多指令支持的前置条件
- 第 3 轮评审后确定的"后端攻坚"战略的核心一步

**实现详情**（`native_backend.py` 重写 `_compile_body` 方法）：

**阶段 A: 线性扫描寄存器分配**
- **活跃区间收集**：遍历所有指令，从 `src_locs` 和 `dst_loc` 中提取虚拟寄存器，记录每个 vreg 的首次和末次使用位置（`first` / `last`）
- **寄存器池**：13 个 GPR（RCX/RDX/RSI/RDI/R8-R15 + RBX/R12-R15） + 8 个 XMM，RAX 预留为特殊用途寄存器
- **线性扫描算法**：按 `first_use` 排序 vreg，逐个分配；分配前回收已过期的寄存器（`last_use < current_first`）
- **溢出策略**：寄存器不足时，选择当前活跃区间中 `last_use` 最远的作为牺牲者，将其溢出到栈槽，新 vreg 占用腾出的寄存器
- 栈帧大小自动更新：溢出时累加 `stack_offset`，写回 `func.stack_size`

**阶段 B: 指令发射链路打通**
- `get_loc(vreg_name)`：获取 vreg 的物理位置（`("reg", phys_reg)` 或 `("stack", offset)`）
- `load_to_reg(vreg, target_reg)`：将 vreg 值加载到目标物理寄存器（处理寄存器搬移和栈加载）
- `store_from_reg(vreg, source_reg)`：将源寄存器值存储到 vreg 位置（处理寄存器搬移和栈存储）

**已适配的指令**：
- `LIRLoadConst`：int/float/bool/string 四种常量，支持寄存器和栈目标
- `LIRBinOp`：整数 +-*/%、浮点 +-*/、所有 6 种比较（整数+浮点）
- `LIRUnaryOp`：整数取反、逻辑非、浮点取反
- `LIRCall`：call 指令 + 返回值存储
- `LIRReturn`：返回值加载到 RAX/XMM0
- `LIRBranch`：条件加载 + test + jne/jmp
- `LIRJump`/`LIRLabel`/`LIRPanic`：保持原有逻辑

**结果**: ✅ 成功，392 测试全部通过，无回归

---

### 测试对比

| 指标 | 开发前 | 开发后 | 变化 |
|------|--------|--------|------|
| 总通过数 | 392 | 392 | ±0 |
| 失败数 | 0 | 0 | ±0 |
| 回归 | - | 无 | - |

---

### 下一步计划

**前端下一步**：
- 优先考虑 `frontend_type_unification`（hard, 95）— 攻坚类型合一算法，这是前端类型系统的核心基石，easy 任务已基本清完
- 或 `frontend_pattern_exhaustiveness`（hard, 82）— 模式匹配完备性检查，提升安全性
- 或 `frontend_map_literal_parse`（medium, 58）— 填补 Map 字面量断层，中等难度功能增益

**后端下一步**：
- 优先考虑 `backend_native_stack_frame`（hard, 90）— 完善栈帧管理，寄存器分配已完成，栈帧是下一个硬骨头（依赖已满足）
- 或 `backend_phi_edge_block_fix`（medium, 82）— 修复 Phi 节点降级的边缘块正确性 bug
- 或 `backend_native_call_abi`（hard, 88）— 实现 System V 调用约定（依赖栈帧，需先做栈帧）

---

## 第 4 轮 — 2026-07-22 16:03 ~ 16:12

### 本轮概览
- **前端任务**: ✅ 修复 ADT 类型参数相等性比较 (easy, 80)
- **后端任务**: ✅ 修复原生后端 ELF 入口地址并接入编译管道 + 函数间调用回填 (medium, 96)
- **测试对比**: 开发前 392 passed → 开发后 392 passed（无回归）

---

### 🎨 前端：修复 ADT 类型参数相等性比较

**为什么选这个？**
- easy 难度，低风险高收益的类型安全修复
- P0 级类型安全漏洞：`ADTType.__eq__` 只比较 name，完全忽略 type_params
- `Option[Int] == Option[String]` 返回 True，所有泛型 ADT 实例类型检查失效
- 第 3 轮评审中明确标记为高优先级 easy 任务，建议优先做

**实现详情**：
- **修复 `ADTType.__eq__`**（`type_checker.py` 第 179-184 行）：
  - 增加 `type_params` 长度检查（长度不同直接不等）
  - 增加逐元素递归比较（`all(p1 == p2 for p1, p2 in zip(...))`）
- **同步修复 `__hash__`**：从 `hash(("ADT", self.name))` 改为 `hash(("ADT", self.name, tuple(self.type_params)))`
  - 确保 `__eq__` 和 `__hash__` 一致，避免 dict/set 行为异常
- **补充 `_types_compatible` 递归检查**：
  - 新增 `MapType` 递归兼容（key_type + value_type）
  - 新增 `TupleType` 递归兼容（长度检查 + 逐元素）
  - 新增 `ADTType` 递归兼容（name + type_params）
  - 与已有的 `FnType`、`ListType` 保持一致设计

**结果**: ✅ 成功，测试全部通过

---

### ⚙️ 后端：修复 ELF 入口地址 + 接入编译管道 + link_calls 回填

**为什么选这个？**
- 优先级 96（所有任务中最高），P0 级 bug
- 第 3 轮评审明确指出原生后端是"僵尸代码"，首要任务就是让它真正工作
- 依赖 `backend_native_label_patch` 已在第 1 轮完成，依赖链畅通
- 三项修复（e_entry、link_calls、接入管道）关联紧密，适合一轮完成
- 是后续寄存器分配、栈帧管理、调用约定等所有原生后端任务的前置条件

**实现详情**：

**1. 修复 ELF e_entry 虚拟地址 bug**（`native_backend.py`）
- 原代码：`entry=start_offset`（文件偏移 0）
- 修复后：`entry=base_addr + start_offset`（虚拟地址 0x400000）
- 将 `page_size` 和 `base_addr` 定义提前到 ELF 头生成之前

**2. 实现 link_calls 函数间调用回填**
- 修改 `link_calls` 数据结构：从 `(code_offset, func_name)` 改为 `(caller_func_name, code_offset_in_func, target_func_name)`
  - 与 `data_fixups` 设计一致，支持多函数场景
- 修改 `_compile_body` 签名：增加 `func_name` 参数
- 修改 `_generate_start` 中三处 `link_calls.append`：增加 `"_start"` 调用者名
- 在 `_generate_elf` 中新增回填逻辑（参照 `data_fixups` 模式）：
  - 计算每个 call 指令在代码段中的绝对偏移
  - 查找目标函数的虚拟地址
  - 计算 rel32 = target_vaddr - (patch_pos_vaddr + 4)
  - 使用 `struct.pack_into` 回填

**3. 接入编译管道**（`compiler_pipeline.py`）
- `BACKEND_NATIVE` 后端从 `CraneliftBackend` 切换为 `NativeCodeGen`
- 适配接口：`compile()` 返回 bytes → 写入文件 + chmod 755
- `--target native` 现在真正使用自研原生后端

**结果**: ✅ 成功，测试全部通过

---

### 测试对比

| 指标 | 开发前 | 开发后 | 变化 |
|------|--------|--------|------|
| 总通过数 | 392 | 392 | ±0 |
| 失败数 | 0 | 0 | ±0 |
| 回归 | - | 无 | - |

---

### 下一步计划

**前端下一步**：
- 优先考虑 `frontend_eq_op_typecheck`（easy, 75）— 修复 `==/!=` 比较运算符类型检查，继续清理 easy 高收益类型安全 bug
- 或 `frontend_type_unification`（hard, 95）— 攻坚类型合一算法，这是前端类型系统的核心瓶颈

**后端下一步**：
- 优先考虑 `backend_native_reg_alloc`（hard, 94）— 攻坚寄存器分配器，让虚拟寄存器真正映射到物理寄存器
- 或 `backend_phi_edge_block_fix`（medium, 82）— 修复 Phi 节点降级的边缘块正确性 bug

---

## 🔍 第 3 轮评审 — 2026-07-22 13:00 ~ 13:15

> 三轮回顾评审：第 1-2 轮总结 + 双线路线图调整

---

### 📊 三轮回顾总结

**完成任务统计：**

| 轨道 | 完成数 | 总任务数（原） | 完成率 |
|------|--------|---------------|--------|
| 🎨 前端 | 2 | 6 | 33.3% |
| ⚙️ 后端 | 2 | 9 | 22.2% |
| **总计** | **4** | **15** | **26.7%** |

**两轮产出质量：** 4/4 全部成功，无失败任务，测试通过率保持 100%（392 passed），无回归。

---

### 🎨 前端线评估

**质量趋势：稳步提升**
- 两轮均为正确性修复（`?` 操作符类型检查、字段访问错误），质量稳步提升
- 类型检查器的错误诊断质量明显改善（从静默吞错到精确提示）
- 但核心机制（类型合一）仍缺失，类型系统整体是"跛脚的"

**进度评估：**
- 完成 2/6 = 33%，但两个都是 easy/medium 的修补任务
- 真正的 hard 任务（类型合一、模式完备性）尚未开始
- **实际进度估计：约 15%**（核心功能完成度远低于任务数完成率）

**价值评估：**

| 任务 | 价值评级 | 说明 |
|------|---------|------|
| 类型合一算法 | ⭐⭐⭐⭐⭐ | 类型系统的基石，没有它 TypeVar = Any |
| 模式匹配完备性 | ⭐⭐⭐⭐ | 安全性高价值功能 |
| ADT 类型参数相等 | ⭐⭐⭐⭐ | 低难度高收益的类型安全修复 |
| `==/!=` 类型检查 | ⭐⭐⭐ | 低难度高收益的 bug 修复 |
| 解析器错误恢复 | ⭐⭐⭐ | 开发体验提升，不影响正确性 |
| Map 字面量解析 | ⭐⭐⭐ | 填补功能断层 |

**最大短板：类型合一算法缺失**
- TypeVar 等价于 Any，泛型函数完全不检查
- ADT 类型参数不参与相等比较（`Option[Int] == Option[String]`）
- 没有替换（substitution）机制，泛型无法实例化
- 这是前端所有后续工作的前提

---

### ⚙️ 后端线评估

**质量趋势：基础架构逐步搭建**
- 两轮完成了标签回填和常量加载，原生后端的基础框架在成型
- 但原生后端整体仍处于"概念验证"阶段，离可用差距巨大

**进度评估：**

| 后端 | 完成度 | 阶段 |
|------|--------|------|
| C 后端 (lir_c_backend) | ~70% | 功能可用阶段，事实上的主力后端 |
| WasmGC 后端 | ~45% | 原型可用阶段，GC 部分名不副实 |
| 原生后端 (native_backend) | ~15% | 概念验证阶段，**且未接入编译管道** |

**重大发现：原生后端是"僵尸代码"**
- `compiler_pipeline.py` 中 `BACKEND_NATIVE` 实际绑定的是 `CraneliftBackend`，而非 `NativeCodeGen`
- ELF 入口地址是严重 bug：使用文件偏移 0 而非虚拟地址 0x400000
- 寄存器分配几乎为零（硬编码 RAX/RCX）
- 函数调用参数传递完全未实现
- `link_calls` 收集了但从未使用（函数间调用地址错误）

**价值评估：**

| 任务 | 价值评级 | 说明 |
|------|---------|------|
| ELF 入口修复 + 接入管道 | ⭐⭐⭐⭐⭐ | P0 bug，决定自研后端是真是假 |
| 寄存器分配 | ⭐⭐⭐⭐⭐ | 后端核心功能，没有就不能用 |
| 函数调用约定 | ⭐⭐⭐⭐⭐ | 不能调用函数的后端没有实用价值 |
| 函数间调用回填 | ⭐⭐⭐⭐ | 多函数程序正确运行的必要条件 |
| Phi 边缘块修复 | ⭐⭐⭐⭐ | 正确性 bug，SSA 语义可能被破坏 |
| Wasm 栈平衡验证 | ⭐⭐⭐ | 正确性基础，复杂程序会栈不平衡 |
| 指令选择优化 | ⭐⭐ | 低难度高收益的代码质量优化 |

**最大短板：原生后端脱离主流水线**
- 宣传与实际严重不符
- 多个 P0 级 bug 共存（入口地址、寄存器分配、调用约定）
- 优先级最高的任务应该是"让原生后端真正工作起来"

---

### 🧭 综合评估

**前后端平衡：严重失衡**
- 前端：核心机制缺失（类型合一），但外围功能相对完整
- 后端：原生后端几乎为零，C 后端尚可
- **后端缺口远大于前端**，建议后续 3 轮后端投入比例 60%，前端 40%

**方向正确性：**
- 前端方向正确，但类型合一这个硬骨头必须啃下来
- 后端方向需要调整：原生后端优先级最高的不是寄存器分配，而是**先接入管道 + 修复 P0 bug**
- 整体方向正确，符合"前端重类型、后端重原生"的定位

**效率评估：**
- 两轮 4 个任务全部成功，效率不错
- 但任务难度偏低（2 easy + 2 medium），hard 任务尚未验证
- 每轮平均产出：2 个中小型任务

---

### 📝 任务池变更说明

**前端任务池变更（6 → 6 个）：**

| 变更 | 任务 | 原因 |
|------|------|------|
| ⬆️ 优先级提升 | 类型合一算法 (90→95) | 确认为前端核心基石，优先级最高 |
| ➕ 新增 | ADT 类型参数相等性修复 | 低难度高收益的类型安全 bug，审计发现 |
| ➕ 新增 | `==/!=` 比较运算符类型检查 | 低难度高收益的类型安全 bug，审计发现 |
| ➕ 新增 | Map 字面量解析与类型检查 | 填补功能断层（AST 有 MapExpr 但 parser 缺失） |
| ➖ 移除 | 收紧 TypeVar 兼容性判断 | 与类型合一是同一个大任务，不应单独存在 |
| ⬇️ 优先级降低 | 解析器错误恢复 (60→60 不变) | 开发体验类，优先级低于正确性 |

**后端任务池变更（9 → 12 个）：**

| 变更 | 任务 | 原因 |
|------|------|------|
| ➕ 新增（P0） | ELF 入口地址修复 + 接入编译管道 | 审计发现的 P0 bug，原生后端是僵尸代码 |
| ➕ 新增 | 函数间调用回填 | link_calls 收集了但未使用，多函数程序错误 |
| ➕ 新增 | Phi 节点降级边缘块修复 | 正确性 bug，条件分支 Phi 拷贝位置错误 |
| ➕ 新增 | Wasm 栈平衡验证器 | 复杂 Wasm 程序栈不平衡，审计发现 |
| ➕ 新增 | 指令选择优化 | 低难度高收益，xor eax,eax 替代 mov rax,0 |
| 🔄 依赖调整 | 寄存器分配 → 依赖 ELF 修复 | 先接入管道再做寄存器分配 |
| ⬆️ 优先级提升 | 函数调用约定 (84→88) | 原生后端可用性的关键 |

---

### 🎯 下 3 轮方向与重点

**总体策略：后端攻坚 + 前端类型安全**

**前端（40% 精力）：**
1. 先做两个 easy 高收益修复（ADT 类型参数相等、`==/!=` 类型检查）
2. 然后全力攻坚类型合一算法（可能需要 2-3 轮）
3. 模式匹配完备性作为类型合一后的下一步

**后端（60% 精力）：**
1. 第 3 轮（下一轮）：修复 ELF 入口 + 接入编译管道（P0）
2. 第 4 轮：函数间调用回填 + Phi 边缘块修复（正确性）
3. 第 5 轮：寄存器分配（核心功能）
4. 后续：栈帧管理 → 调用约定 → 指令选择优化

---

### 更新后的进度概览

| 轨道 | 总数 | 已完成 | 进行中 | 完成率 |
|------|------|--------|--------|--------|
| 🎨 前端 | 6 | 2 | 0 | 33.3% |
| ⚙️ 后端 | 12 | 2 | 0 | 16.7% |
| **总计** | **18** | **4** | **0** | **22.2%** |

---

## 第 2 轮 — 2026-07-22 10:01 ~ 10:15

### 本轮概览
- **前端任务**: ✅ 修复字段访问异常静默吞噬 (easy, 65)
- **后端任务**: ✅ 实现原生后端浮点/字符串常量加载 (medium, 75)
- **测试对比**: 开发前 392 passed → 开发后 392 passed（无回归）

---

### 🎨 前端：修复字段访问异常静默吞噬

**为什么选这个？**
- easy 难度，快速修复已知质量缺陷，投入产出比高
- `_check_field_access` 中 `ValueError` 被静默 `pass`，错误信息也不准确（非数字索引也报"越界"）
- 属于类型检查器的正确性修复，提升错误诊断质量
- 上轮下一步计划中明确提到优先考虑此任务

**实现详情**：
- 重写 `_check_field_access` 方法（`type_checker.py`），消除所有静默异常吞噬
- **元组数字索引**：明确区分三种情况
  - 索引格式无效（非数字）→ 报"元组访问需要数字索引"，附使用提示
  - 索引越界 → 报"元组索引 N 越界"，附带元组长度和有效范围
  - 索引有效 → 返回对应元素类型
- **ADT 类型**：明确提示 ADT 类型需通过模式匹配访问字段，而非直接 `.field`
- **其他类型**：给出精确的"不支持字段访问"错误和提示
- 所有错误路径均有明确的 `TypeCheckError` 抛出，无不透明吞错

**结果**: ✅ 成功，测试全部通过

---

### ⚙️ 后端：实现原生后端浮点/字符串常量加载

**为什么选这个？**
- 依赖 `backend_native_label_patch` 已在上轮完成，依赖链畅通
- 原生后端当前只能处理整数，浮点和字符串常量都是 `pass` 占位
- x86_64 指令编码器（`x86_64.py`）已有 `movsd_reg_imm` 和 `lea_reg_rip`，基础设施就绪
- 中等难度，是原生后端从"整数原型"迈向"可用"的重要一步
- 上轮下一步计划中明确提到优先考虑此任务

**实现详情**：
- **常量收集优化**（`_collect_constants`）：增加去重逻辑，相同浮点值/字符串只存一份到数据段，构建 `_float_const_map` 和 `_string_const_map` 快速查找表
- **浮点常量加载**（`LIRLoadConst` float 分支）：
  - 调用 `e.movsd_reg_imm(reg, 0)` 发射 `movsd xmm, [rip + offset]` 指令
  - 记录 `data_fixups` 项：`(func_name, fixup_offset, data_offset, "float")`
- **字符串常量加载**（`LIRLoadConst` string 分支）：
  - 调用 `e.lea_reg_rip(reg, 0)` 发射 `lea reg, [rip + offset]` 指令
  - 记录 `data_fixups` 项：`(func_name, fixup_offset, data_offset, "string")`
- **ELF 阶段回填**（`_generate_elf`）：
  - 计算数据段虚拟地址（按页对齐）
  - 遍历 `data_fixups`，计算 RIP-relative 偏移 = 目标地址 - (rel32位置 + 4)
  - 使用 `struct.pack_into` 回填到代码段

**结果**: ✅ 成功，测试全部通过

---

### 测试对比

| 指标 | 开发前 | 开发后 | 变化 |
|------|--------|--------|------|
| 总通过数 | 392 | 392 | ±0 |
| 失败数 | 0 | 0 | ±0 |
| 回归 | - | 无 | - |

---

### 下一步计划

**前端下一步**：
- 优先考虑 `frontend_type_unification`（hard, 90）— 攻坚类型合一算法，这是前端类型系统的核心瓶颈
- 或 `frontend_pattern_exhaustiveness`（hard, 82）— 模式匹配完备性检查，提升静态检查质量

**后端下一步**：
- 优先考虑 `backend_native_reg_alloc`（hard, 88）— 攻坚寄存器分配器，让虚拟寄存器真正映射到物理寄存器
- 或 `backend_wasm_indirect_multiarg`（medium, 72）— Wasm 后端多参数闭包调用，完善 Wasm 后端功能

---

## 第 1 轮 — 2026-07-22 08:14 ~ 08:17

### 本轮概览
- **前端任务**: ✅ 完善 ? 操作符类型检查 (medium, 78)
- **后端任务**: ✅ 实现原生后端两阶段汇编与标签回填 (medium, 92)
- **测试对比**: 开发前 392 passed → 开发后 392 passed（无回归）

---

### 🎨 前端：完善 ? 操作符类型检查

**为什么选这个？**
- 优先级 78（中等偏高），不依赖其他任务，第一轮有把握完成
- 当前 `_check_try_expr` 是完全的占位实现（只有一行 return），任何类型都可以加 `?`
- 修复后能明显提升类型安全性，投入产出比高

**实现详情**：
- 重写 `_check_try_expr` 方法（`type_checker.py`）
- 检查内部表达式是否为 `Option` 或 `Result` 的 ADT 类型
- `Option[T]?` → 返回 `T`；`Result[T, E]?` → 返回 `T`
- 非 Option/Result 类型使用 `?` 报 `TypeCheckError`
- TypeVar 暂时放行（在合一算法实现前保持兼容性）

**结果**: ✅ 成功，测试全部通过

---

### ⚙️ 后端：实现原生后端两阶段汇编与标签回填

**为什么选这个？**
- 优先级 92（后端最高），是原生后端所有其他功能的基石
- 没有标签回填，控制流（jump/branch）全部跳到错误地址
- emitter 层（`x86_64.py`）已有 `patch_rel32` 等基础设施，缺的是调度逻辑
- 是后续寄存器分配、栈帧管理、ABI 等任务的前置依赖

**实现详情**：
- 在 `_compile_body` 中引入两阶段汇编架构
- **第一阶段**：发射指令，同时维护 `label_offsets`（标签名→偏移）和 `jump_fixups`（待回填跳转列表）
  - `LIRLabel`：记录 `e.current_offset()` 到 `label_offsets`
  - `LIRJump`：发射 `jmp_rel32` 占位，记录 `(offset, target, "jmp")`
  - `LIRBranch`：发射 `test + jne + jmp`，分别记录两个回填项
- **第二阶段**：遍历 `jump_fixups`，调用 `e.patch_rel32(offset, target_offset)` 回填

**结果**: ✅ 成功，测试全部通过

---

### 测试对比

| 指标 | 开发前 | 开发后 | 变化 |
|------|--------|--------|------|
| 总通过数 | 392 | 392 | ±0 |
| 失败数 | 0 | 0 | ±0 |
| 回归 | - | 无 | - |

---

### 下一步计划

**前端下一步**：
- 优先考虑 `frontend_field_access_error`（easy, 65）— 快速修复字段访问异常静默吞噬
- 或 `frontend_type_unification`（hard, 90）— 攻坚类型合一算法，为后续 TypeVar 收紧打基础

**后端下一步**：
- 优先考虑 `backend_native_float_const`（medium, 75）— 依赖已满足，实现浮点/字符串常量加载
- 或 `backend_native_reg_alloc`（hard, 88）— 攻坚寄存器分配器，让虚拟寄存器真正工作

---

*（第 0 轮：初始状态，等待第 1 轮开始）*
